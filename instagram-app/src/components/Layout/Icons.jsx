import React               from 'react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const Icons = ({arrayIcon}) => <FontAwesomeIcon icon={ arrayIcon }/>;

export default Icons;